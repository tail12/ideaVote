<% if current_user.likes.find_by(idea_id: idea.id) %>
    <%= link_to 'イイネ取り消す', unlike_path(idea.id),
                class: 'btn btn-waring btn-sm like',
                method: :delete,
                remote: true %>
<% else %>
    <%= link_to 'イイネ', like_path(idea.id),
                class: 'btn btn-warning btn-sm like',
                method: :post,
                remote: true %>
<% end %>

